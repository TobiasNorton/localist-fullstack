{
  "private": true,
  "cacheDirectories": [
    "client/node_modules"
  ],
  "scripts": {
    "build": "npm run build && npm run deploy",
    "deploy": "cp -a client/build/. public/"
  },
  "heroku-run-build-script": true
}
