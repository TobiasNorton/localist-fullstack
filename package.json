{
  "private": true,
  "cacheDirectories": [
    "client/node_modules"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "postbuild": "cp -a client/build/. public/"
  },
  "heroku-run-build-script": true
}
